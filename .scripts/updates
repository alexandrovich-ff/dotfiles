#!/bin/sh
# Syncs repositories and downloads updates, meant to be run as a cronjob.

ping -q -c 1 1.1.1.1 > /dev/null || exit

notify-send "📦 Обновление репозитория" "Проверка наличия обновлений..."

sudo pacman -Syyuw --noconfirm || notify-send "Ошибка при установке обновлений.
Проверьте подключение к Интернету. Если pacman уже запущен - запустите обновление вручную, чтобы увидеть ошибки."

if pacman -Qu | grep -v "\[ignored\]"
then
	notify-send "🎁 Репозиторий обновлен" "Есть пакеты которые нуждаются в обновлении. Установка (📦) ..."
else
	notify-send "📦 Репозиторий обновлен"  "Система не нуждается в обновлении."
fi
